"""Framework integrations for Hopframe.

Each module here imports its framework lazily and exposes a single
adapter (callback handler, decorator, or wrapper). Importing
hopframe.integrations does not import any third-party deps.
"""
